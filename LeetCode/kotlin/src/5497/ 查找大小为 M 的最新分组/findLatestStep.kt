package `5497`

class Solution {
    fun findLatestStep(arr: IntArray, m: Int): Int {
        val amap = mapOf(0 to arr.size)
        var res = -1
        for (i in arr.lastIndex downTo 0){
            val k = arr[i]

        }
        return res
    }
}